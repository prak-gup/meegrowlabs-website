# Netlify redirects file
# Fallback to index.html for React SPA routing
# This ensures all routes (except actual files) are handled by the React app
/postiz/*  /social/:splat  301
/* /index.html 200

